machine:
  services:
    - neo4j
general:
  build_dir: src/server
dependencies:
  override:
    - npm install
deployment:
  production:
    branch: master
    commands:
      - ./deployment/aws/circleci_deploy_prod.sh

